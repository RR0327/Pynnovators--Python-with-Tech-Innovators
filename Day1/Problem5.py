# 5. Assign values to three variables in one line and print a formatted sentence. Then assign one value to multiple variables in a single line.

x, y, z = 5, 10, 15
print("Values:", x, y, z)

p = q = r = 100
print("Same values:", p, q, r)

# Standard way

a, b, c = 10, 20, 30
print(f"Values are a={a}, b={b}, c={c}")

x = y = z = 100
print("x =", x, "y =", y, "z =", z)
